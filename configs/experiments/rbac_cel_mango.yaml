admin:
  address:
    socket_address:
      protocol: TCP
      address: 127.0.0.1
      port_value: 9901
static_resources:
  listeners:
  - name: localhost-listener
    address:
      socket_address:
        protocol: TCP
        address: 127.0.0.1
        port_value: 7777
    filter_chains:
    - filters:
      - name: envoy.filters.network.http_connection_manager
        typed_config:
          "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
          stat_prefix: ingress_http
          access_log:
          - name: envoy.access_loggers.stdout
            typed_config:
              "@type": type.googleapis.com/envoy.extensions.access_loggers.stream.v3.StdoutAccessLog
          route_config:
            name: local_route
            virtual_hosts:
            - name: local_service
              domains: ["*"]
              routes:
              - match:
                  prefix: "/"
                route:
                  cluster: admin_cluster
#              request_headers_to_add:
#                - header:
#                    key: peach
#                    value: melba
#                  append: true
          http_filters:
#          - name: rbac-deny-if-header-peach-is-null
#            typed_config:
#              "@type": type.googleapis.com/envoy.extensions.filters.http.rbac.v3.RBAC
#              rules:
#                action: ALLOW
#                policies:
#                  "policy-rbac-deny-if-header-peach-is-null":
#                    # deny if request.headers[peach].equals("")
#                    condition:
#                      call_expr:
#                        function: _==_
#                        args:
#                        - select_expr:
#                            operand:
#                              select_expr:
#                                operand:
#                                  ident_expr:
#                                    name: request
#                                field: headers
#                            field: peach
#                        - const_expr:
#                            string_value: melba
#                    permissions:
#                    - any: true
#                    principals:
#                    - any: true
          - name: rbac-deny-if-mango-is-empty-3
            typed_config:
              "@type": type.googleapis.com/envoy.extensions.filters.http.rbac.v3.RBAC
              rules:
                action: ALLOW
                policies:
                  "policy-rbac-deny-if-mango-is-empty-3":
                    # deny if has(request[mango]) == true
                    condition:
                      select_expr:
                        operand:
                          ident_expr:
                            name: request
                        field: mango
                        test_only: true
                    permissions:
                    - any: true
                    principals:
                    - any: true
#          - name: rbac-deny-if-peach-is-empty-4
#            typed_config:
#              "@type": type.googleapis.com/envoy.extensions.filters.http.rbac.v3.RBAC
#              rules:
#                action: ALLOW
#                policies:
#                  "policy-rbac-deny-if-peach-is-empty-4":
#                    # deny if has(request[peach]) == true
#                    condition:
#                      select_expr:
#                        operand:
#                          select_expr:
#                            operand:
#                              ident_expr:
#                                name: request
#                            field: headers
#                        field: peach
#                        test_only: true
#                    permissions:
#                    - any: true
#                    principals:
#                    - any: true
          - name: envoy.filters.http.router
  clusters:
  - name: admin_cluster
    type: strict_dns
    lb_policy: ROUND_ROBIN
    load_assignment:
      cluster_name: admin_cluster
      endpoints:
      - lb_endpoints:
        - endpoint:
            address:
              socket_address:
                address: 127.0.0.1
                port_value: 9901

